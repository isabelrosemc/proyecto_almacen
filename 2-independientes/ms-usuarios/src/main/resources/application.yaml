server:
  port: 8081

spring:
  application:
    name: ms-usuarios

  datasource:
    url: jdbc:mysql://localhost:3306/db_usuarios
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO