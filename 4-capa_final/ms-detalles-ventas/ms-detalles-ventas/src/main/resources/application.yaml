server:
  port: 8090

spring:
  application:
    name: ms-detalle-ventas

  datasource:
    url: jdbc:mysql://localhost:3306/db_detalle_ventas
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka