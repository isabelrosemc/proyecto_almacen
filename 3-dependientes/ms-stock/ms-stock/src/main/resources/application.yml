server:
  port: 8085

spring:
  application:
    name: ms-stock

  datasource:
    url: jdbc:mysql://localhost:3306/db_stock
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    com.almacen.ms_stock: DEBUG