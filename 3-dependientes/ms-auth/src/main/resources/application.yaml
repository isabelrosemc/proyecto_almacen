server:
  port: 8085

spring:
  application:
    name: ms-auth

logging:
  level:
    root: INFO
    com.almacen.msauth: DEBUG

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true

jwt:
  secret: miclavesupersecretajwt2026almacenfullstack123456
  expiration: 3600000